extends Node

var numero_video = '1'; # el numero del video que se va a mostrar
var numero_pregunta = '1'; # el numero de la pregunta que se va a mostrar
var nivel = '1'; # el nivel seleccionado por el jugador

# datos del jugador
var nombre_jugador; # el nombre seleccionado por el jugador
var grado_jugador; # el nombre seleccionado por el jugador
var student_data: Dictionary;

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
